// Boolean
const shirtIsBLue = true;
const shirtIsRed = false;

// Conditionals are used to verify if a situation is true or false
const number = 10;

const isEven = number % 2 == 0;
console.log(isEven);

// In javascript the number inside ""is a string. In a comparison it is transformed to the type needed to compare.

// Exclamation used to invert the value
if (!isEven) {
    console.log("Here");
}

if (isEven) {
    console.log("Par");
} else {
    console.log("Impar");
}

